Lecture 1: Introduction to Backend 

How JS is run? 
It is run with the help of v8 engine.
What is Node js? 
You can use JS anywhere with the help of this. 
What is V8 engine?
https://v8.dev/
A program which is written using c++ language to run JS. It can be embedded to any c++ application. 
If it is made using c++ language then how come it understands the JS code?
How a frontend gets the data from database?
We use server for getting data from database
Why do we put server between Database and Frontend? 
We don't want to expose our database because frontend it exposed to everyone so to deal with this issue, we don't expose it.
Because business logics are written in the database and other important and sensitive details. 
What is global object and how it gives extra power to v8 engine? 
Global Object:
	setTimeOut
	setInterval
	fetch()
	console.log()
	DOM
	How DB is connected.
Why v8 is not made in any other language? 
Is the code of JS converted into c++ before running? 
no, JS code is taken as an input for running.
How to js files can be run together?
The code of 2nd file can be written in the first file using requie("./second") => common js module
If a function is written in the second file so it can be used directly in the first file? 
No, it is not permitted without export. 
IIFE => Immediately Invoked Function Expression
	=> It works like IIFE.
The whole function is wrap up in brackets and that's why you have to call immediately just after written

the use of module.experts for exporting function.
if you want to export more than one so these can be exported using an object. 
Doing console.log() and looking for what it is actually and finding different ways for exporting these functions.
common js module => it is the older method for exporting. The new one is import and export but these old methods are still mostly used in the industry. 
Node js => by default follows CJS 
MJS => Import export => this is not the behavior of the node
that's why by changing the extension of the file to .mjs so now it can understand it.
These things are managed by bundler in React or parcel that's why there these are used directly without changing the .mjs 
This work can be done just by typing {type: "module"} in package.json

so if you want to use MJS, type will be "module" and CJS can be run using "commonjs" in module